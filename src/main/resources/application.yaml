spring:
  jpa:
    defer-datasource-initialization: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
oss:
  prefix: http://127.0.0.1:9000/${oss.minio.bucket}/
  strategy: minio
  minio:
    bucket: bootstrap
    credentials:
      access-key: jiahua-lan
      secret-key: ZxC3177468
    endpoint: http://127.0.0.1:9000
